name: archisri_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  percent_indicator: ^4.2.3
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  google_sign_in: ^6.2.1
  firebase_core_web: ^3.5.0
  firebase_auth_web: ^6.1.3
  cloud_firestore: ^6.1.3
  url_launcher: ^6.3.1
  http: ^1.1.0
  firebase_database: ^12.1.4
  file_saver: ^0.3.1
  file_picker: ^8.0.0
  gal: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: false
  image_path: assets/images/ArchiSrilogo.png
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: assets/images/ArchiSrilogo.png

flutter:
  uses-material-design: true

  assets:
    - assets/images/
